// Seeded by supabase/migrations/..._question_sets_and_aspects.sql — the
// "Alap kérdések" set every new partner defaults to and that can never be
// deleted, only edited.
export const DEFAULT_QUESTION_SET_ID = "00000000-0000-0000-0000-000000000001";
